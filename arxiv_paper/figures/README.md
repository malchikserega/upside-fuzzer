# Figures Directory

This directory contains (or will contain) the graphical assets for the LaTeX paper compilation:

1. `architecture.pdf` (or .png / .jpg) - The End-to-End architecture diagram
2. `coverage_graph.png` - The benchmark results showing Edge Coverage over time

**Next Steps**:
Once the 24-hour evaluation runs are complete, extract the graph data and plot it (e.g., using matplotlib or Excel) and save it here as `coverage_graph.png` before compiling `paper.tex`. You can generate `architecture.pdf` by exporting the Mermaid diagram from `paper.md`.
